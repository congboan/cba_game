# 2026-07-30

## ban_engine_path.py 重构
- 删除 6 个硬编码引擎路径，改为动态调用 resolve_engine() 获取本机真实引擎路径
- 二层检测：精确匹配本机路径 + 正则兜底任何盘符+/Engine/的绝对路径
- 分步 Edit 导致半写死锁：import 已更新但 main() 仍引用已删除的 FORBIDDEN_ENGINE_PATHS → NameError → exit 1 → 误拦截一切编辑
- 用户手动解锁后修复 REPO_ROOT 路径（checks/ 比 skill 根深一层，需四层 ../ 而非三层）
- 验证通过：Edit 和 Write 均可正常工作

## 代码规范门禁
- 新增 `checks/check_generated_include.py`：检测 `.cpp` 中 `.generated.h` 是否是最后一条 `#include`
- 新增 `checks/check_ue_naming.py`：检测 `UCLASS`/`USTRUCT`/`UENUM`/`UINTERFACE` 的命名前缀（U/A、F、E）
- SKILL.md 新增 2 条约束：`project.code.generated-include-last` + `project.code.ue-naming-prefix`
- 约束总数：16 条

## 自主开发流水线
- Stage 从 design 切到 build，解锁 C++ 写入
- SKILL.md 重写为「Autonomous Development Pipeline」：完全自主闭环（Planner→Coder→Reviewer→循环迭代）
- Agent 规则翻转为全自主：AI 可 spawn Planner/Coder/Reviewer/Explorer，不设迭代次数上限
- 审查分三级：Blocker（必修）/ Style（应修）/ Suggestion（建议）
- 交付条件：Blocker 全清 + Style 处理完毕；Suggestion 可留给用户
- harness_state.json 清空 active_skills，进入开发模式
