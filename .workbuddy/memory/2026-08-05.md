# 2026-08-05 工作日志

## Harness git 归一化治理修改（governance-authoring 流程）

背景：git add 被 HARNESS_OPERATION_UNRESOLVED 保守拒绝；GIT_NON_WORKTREE
把 add/rm 标无副作用放行（git rm 删文件是虚假放行漏洞）。

修改（宿主 harness/，常驻）：operation_normalizer.py
- GIT_WORKTREE_WRITE（add/rm/restore/commit）→ 解析路径为精确 pre_write
- add 无路径 → 仓库根 . ；GIT_WORKTREE_UNRESOLVED（checkout/merge/rebase 等）
- NON_WORKTREE 移除 add/rm/reset；支持 git -C 绝对路径；commit 单次 pre_commit

decisions.md 新增 ADR（2026-08-05）；README 同步。验证：40 项 ALL PASS
（临时 smoke 脚本走真实 hook 链路，验证后删除）。真实 hook：git status / -C /
diff 放行；--context Exit 0；state 复位 active_skills 清空。

关键发现：git 写 .git/ 失败（add: unable to write index；commit: .git/logs/HEAD
Permission denied），但 bash touch .git/index 成功（含非沙箱）→ WorkBuddy 派生进程
git.exe 写 .git 受限，harness 不替代 OS 权限，非治理问题；用户终端手动 git 正常。

其他：WorkBuddy hook 长 Write/Edit payload 序列化 bug（stdin JSON 480+ 列截断 →
invalid_json abort），规避：分段小写入；rm 临时文件需反斜杠路径 D:\\ 才通过
genie-trash；TaskCreate 等任务管理工具未建模被 harness 拒（后续治理扩展候选）。

## 提交困境（13:04 追加）

git add/commit 在 WorkBuddy 环境全部失败，证据链：
- git add: fatal: unable to write new index file（harness 已放行，git 自身 OS 层失败）
- git commit: cannot update ref HEAD: .git/logs/HEAD Permission denied
- bash touch .git/index、.git/index.lock、.git/index.lock.test 全部成功（含非沙箱）
- bash mv .git/... 被 harness 拒（保护 .git 内部，正确行为）
- git rev-parse --git-path index/logs/HEAD 正常（路径解析无问题）
- git status 能读，git ls-files 能读 → git 进程有读权限

结论：WorkBuddy 派生的 bash 进程能写 .git（touch OK），但 git.exe 进程写被 OS 拒
（unable to write / Permission denied）。这是 WorkBuddy 进程模型下 git.exe 的
权限/令牌差异，非 harness 治理问题（harness 已正确放行并保护 .git）。

遗留：.git/index.lock.test 测试残留文件需用户终端清理。
提交由用户终端执行（已验证可行）。

## 根因修正：.git/index.lock 残留（13:06）

ls -la .git 显示 index.lock 真实存在（0 字节，13:00 创建）。

主因：lock 残留阻塞 git 写（bash touch 不碰 lock 是假象）；
git 读不受影响；疑 IDE 后台 git 遗留；其他电脑无此问题。
用户终端清理：rm -f .git/index.lock .git/index.lock.test .git/.write_test

## 提交困境根因修正（13:06 追加）

关键证据：ls -la .git 显示 .git/index.lock 真实存在（0 字节，13:00 创建）。

## 根因收敛（次日 13:28 追加）

用户反馈：其他项目无此问题；cmd 里 git add cba_game.uproject 成功。

决定性证据链（同一天内完成）：
- cmd 手动 git add 成功 → .git 目录与 git 本身完全正常
- WorkBuddy 环境内 git add 失败（unable to write new index file），lock 已删仍失败
- git -c core.splitIndex=false add 报精确错误：Unable to create '.../index.lock': Permission denied
  → 定位：git.exe 在 .git/ 根目录创建新文件被 OS 拒（不是 index 写入本身）
- git branch __wtest__ 成功（.git/refs/ 可创建新文件）→ 不是 .git 整体写受限
- git branch -d __wtest__ 失败：.git/logs/refs/heads/__wtest__ unlink Permission denied
  → .git/logs/ 删除被拒
- bash touch .git/.write_test 成功（此前）→ bash 写 .git 根正常，git.exe 不行

模式：同一进程链下 bash 能写 .git/，git.exe 部分目录被 OS 拒（.git/ 根创建、.git/logs/ 删除），
refs/ 可写。+ 用户终端 git 完全正常 → 结论：WorkBuddy 派生进程对原生 git.exe 的
路径级写限制（或安全软件对原生进程规则），harness 不涉入，非治理问题。

测试残留（需用户终端清理）：
- .git/logs/refs/heads/__wtest__（分支已删 refs 已清，reflog 残留）
- .git/.write_test / .git/index.lock.test（早期 touch 测试）
用户终端命令：cd D:\CBA\cba_game && rm -f .git/logs/refs/heads/__wtest__ .git/.write_test .git/index.lock.test

## 对照实验结论（13:44 追加）

对照项目 D:\CBA\HarnessEngineeringTest 内 AI 13:38 提交成功，推翻进程整体受限。
WorkBuddy bash 用 MSYS2 git，cmd 用标准 Git；标准 git 在 cba_game add 成功。
commit 仍失败：.git/COMMIT_EDITMSG 残留 "test empty commit" 被锁，需终端删除。

## 治理写入：hook stdin JSON 截断（13:45）

按 governance-authoring 流程写入两层：
1. harness/decisions.md 新增 ADR（客户端协议边界，harness 无法机制内修复）
2. 根 skill 认知区追加规避规范（分段小 Edit、短 ASCII、abort 不绕过）
流程：预检 → state 激活 governance-authoring → 分段小 Edit → context 验证 → 复位。
实测：Edit 超约 700 字符即触发 hook invalid_json abort。
