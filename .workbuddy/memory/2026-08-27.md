# 2026-08-27

## AAF 审阅与 spec 起草
- 全量审阅 AssemblyAbilityFramework（37 文件）：双层架构、EventRouter 设计正确；问题清单见对话（热路径 LoadSynchronous、链上下文死字段、ini 残留、Validator 未接线等）。
- 对照 LyraGameplayAbility（D:\UEProjects\LyraStarterGame）确认四个缺口：OnSpawn 守卫、失败消息总线、成本对象化、激活组。
- 读 Mover 插件源码获范式印证：四轴正交、单活跃状态机、Transition 首胜、MixMode/Priority 中央裁决、cancel-by-tag。据此新发现 P2 隐患：ResolveExecutionData 遍历 TMap 序不可复现。
- 设计决策：否决 Fragment 全虚函数镜像；采用具名插槽组合；Row 即运行时真相源，授予期预载替代编辑器 bake。
- 走 governance-authoring 流程起草 specs/aaf-named-slots.md（Phase1 正确性 / Phase2 死重清理 / Phase3 CostPolicy 插槽），status: draft，待用户确认后激活。期间遇两次 stdin 截断 abort（截断点 727/5040 列）与一次 authoring-required deny（已按路由激活 governance-authoring 后完成写入并恢复 selector）。

## AAF 构建失败修复（build_freshness 门禁）
- Stop hook 报 `no successful build matching current source`：根因是上次 build_editor.py 失败（exit 6）无成功指纹，且工作区大量 AAF 源码变更。
- 修复 4 类编译错误（见 Saved/harness_build_last.log）：
  1. FAssemblyCostBase.h：`FGameplayAbilitySpecHandle` 仅前向声明被按值用作参数（C2027）→ include GameplayAbilitySpecHandle.h、删前向声明；
  2. AssemblyAbilityStatics.cpp:25：`ASC->IsNetAuthority()` 非 GAS 成员（C2039）→ 改 `GetOwnerActor()->HasAuthority()`；
  3. AOE.cpp:106 / InstantGE.cpp:52：`GECDO` 未声明（C2065）→ 改为 `LoadedGE`（else 分支已加载的 GE 对象，笔误）；
  4. InstantGE.cpp:28：`LogAssemblyAbility` 未声明 → include AssemblyAbilityFrameworkLog.h（AOE.cpp 已有）。
- 修复后重跑 `python harness/scripts/build_editor.py`（后台），成功即更新 Saved/harness_last_build.json 指纹、门禁放行。

## AAF 构建失败修复（第三轮，15:30 失败 exit 6）
- 15:30:24 构建失败根因（Saved/harness_build_last.log）：
  1. UHT 错误：UGA_AssemblyDataDriven.h 嵌套普通 struct `FResponderBinding` 被用作 `UPROPERTY TArray` 元素 → 去 UPROPERTY（含 FDelegateHandle 不可反射，本就该纯 C++ 成员）；
  2. FAssemblyFragmentBase.h：`FGameplayAbilitySpecHandle` 前向声明被按值用（同 FAssemblyCostBase 复发）→ include GameplayAbilitySpecHandle.h；`UAssemblyAbilitySource` 未声明 → 补前向声明；
  3. UGA_AssemblyDataDriven.cpp:186 `AreAbilityTokensBlocked`（不存在）→ `AreAbilityTagsBlocked`（AbilitySystemComponent.h:1083）；
  4. :350 `IsNetAuthority()`（不存在）→ `GetOwnerActor()->HasAuthority()`（带 null 保护）；
  5. :374 `GetTriggers()`（不存在）→ `AbilityTriggers`（protected，须 Cast 到 UGA_AssemblyDataDriven 后访问，C2248）。
- 15:34:38 用户侧构建成功（exit 0，指纹 9e0871bd 匹配当前源码），门禁放行。
- 经验：嵌套普通 struct 不能作 UPROPERTY 类型；FGameplayAbilitySpecHandle 按值参数必须 include 完整定义（本插件已两次踩坑）。

## 决策追记（下午）
- 设计目的定案：JSON 等格式动态创建 GA（类只承载机制；绑 CDO 项=冲突）
- 聚合定案：Row.Fragments 单数组替代多插槽；冷却片经 GetCooldownGameplayEffect 漏斗收敛引擎路径
- N6-B RespondToTags、N5 AssetTags 升必做；N7 选双端对称 Give 契约；队列 X-1/X-2'/B1/B2；待拍板 B3/A2
- 平台缺陷新形态：Agent 工具无 adapter 被拒；根技能门禁脚本自身被 stdin 截断击穿一次（exit2）；纠正一次虚报（Mover 轮声称 MEMORY.md 已写实际未执行，本日已真实补写）

## 决策追记（下午续：Trigger 片终稿 + 生命周期钩子）
- Trigger 片三易其稿终局：`Spec.DynamicAbilityTriggers`（GameplayAbilitySpec.h:254 public 字段）——引擎 GiveAbility:578 自动注册/:637 自动注销，payload 原生投递；自建订阅机件全删（lambda/簿记/pending/OnGrant 双循环归零）。关键实证：`RegisterAbilityTriggers` 在 5.9 是 private（:1273 后）无公开 API，正确通道是 Spec 字段
- 管线去具体类型：Statics 的 `FAssemblyFragment_Trigger::CollectTriggerData` 直调（反模式）→ 基类 `ContributeToSpec(Spec)` 钩子，管线零具体类型引用
- 生命周期钩子窄基类定案（用户设计）：基类最小化（Check/Apply/Precache/ContributeToSpec 四件）+ 8 个独立窄基类（OnGiveAbility/OnRemoveAbility/OnAvatarSet/OnCanActivate/OnPreActivate/OnActivated/OnEndAbility/OnFailed），片按需继承；`ForEachHook<HookT>` 按 UScriptStruct IsChildOf 分派只遍历命中组（USTRUCT 单继承 + GetPtr 已含 reinterpret 语义，显式 cast 安全）；否决探测位虚函数（AsOnXxx 仍是基类膨胀）与 TMap 索引/拍平调用表优化（低频钩子下 R2）
- 本轮落地：FragmentBase.h + 8 窄基类 + ForEachHook；DataDriven 新增 OnGive/OnRemove/OnAvatarSet/PreActivate/EndAbility 五个转发 override（CanActivate 内嵌 OnCanActivate + 失败统一出口 Fail lambda 广播 OnFailed）；编译绿指纹 5e74d2f3（17:27）；spec 勾稽已回填

## 决策追记（傍晚批：硬编码迁移成片 + R1 清理）
- FAssemblyFragment_ActivationGate 片（OnCanActivate 钩子）：BlockedTags（承接原行字段 ActivationBlockedTags，Row 已删该字段）+ RequiredTags（白拿引擎语义）+ bCheckAssetBlock（承接 B1 AssetTags 全局屏蔽，从 GA 读 Row）；CancelAbilitiesWithTags 保持行级编排参数（Commit 前互斥，非结算，不迁）
- DataDriven::CanActivateAbility 收敛为：Super + Fail lambda（OnFailed 广播）+ OnCanActivate 门卫转发——硬编码行为清零
- R1 清账三件：#if 0 遗留冷却段物理删除（含 Row Legacy 四字段一并移除）、FAssemblyCostBase.h 物理删除、DataDriven.h 过时头注释更新
- 编译绿指纹 61930a8c（315 文件，18:35）；spec 勾稽更新；selector 恢复确认
- 环境缺陷新记录：stdin 截断对含特定 CJK 行（含 %s 转义/全角冒号/箭头字符）的 Edit 载荷高复发（连续 10+ 次 abort，列号 534-1170 随机）；绕行法=先把 CJK 子串用小载荷逐字替换为 ASCII，再整块删除，成功率高

## 决策追记（傍晚续：分派查表化定案）
- ForEachHook 遍历版 → 查表版：Source::HookIndices（TMap<UScriptStruct*, TArray<int32>>，惰性构建）+ ForEachHook<HookT>(Source) O(1) 定位桶、遍历命中组；9 处 DataDriven 调用点从 Row->Fragments 切到 Source；编译绿 1541ba6f（18:46）
- 关键澄清（用户追问驱动）：桶键必须是**钩子窄基类类型**（非片具体类型）——A/B 两片同继承 OnEndAbilityBase 时 IsChildOf 归入同一桶，查询 HookT::StaticStruct() 精确命中；片继承多钩子进多桶
- 低频/高频双区分区**判定无意义**（用户结论）：TMap 查找同为 O(1)，成本差异在钩子自身调用频率与存储结构无关；频率自述虚函数/静态成员、双桶区全部砍掉——单表定案
- spec 同步更新（分派查表化条目）；selector 恢复确认
- CompiledRow 封装定案（用户指令）：新增 FAssemblyAbilityCompiledRow（Row 快照 + HookIndices + GetHookIndices 模板），UAssemblyAbilitySource 只留 CompiledRow + OriginSource 两字段（Source 保持干净）；5 处引用点迁移（Statics 赋值/预载/贡献、DataDriven AbilityTag/GetRow、Cooldown 日志、DataDriven.h 注释）；编译绿 d860d34d（19:02）
- 结算族窄基类（用户指令：cost 就 cost 要体现）：FAssemblyFragmentCostBase（Check/Apply 从基类下沉，体现 CheckCost/ApplyCost 管线语义）；基类仅剩授予期工具（Precache/ContributeToSpec）零行为虚函数；Cooldown 改继承 CostBase（冷却=成本管线一员）；CheckCost/ApplyCost 改 ForEachHook<CostBase> 查表；InputTag 删除（暂不需要）；编译绿 0d2a6da9（19:13）
- 架构终态：FAssemblyFragmentBase=纯授予期工具 → 片继承窄基类（CostBase 结算 / 8 生命周期钩子 / 授予贡献）→ ForEachHook 按类型 O(1) 查表分派 → DataDriven 纯编排转发
- Gate 片 tag 语义修正：引擎 DoesAbilitySatisfyTagRequirements（GameplayAbility.cpp:349-443）用 GetOwnedGameplayTags().HasAny/HasAll 精确匹配；Gate 片原用 HasAnyMatching/HasAllMatching（父链展开版）语义不一致 → 已改同款；编译绿 ad943758（19:33）
- 身份子系统定案（用户多轮引导，实证链闭合）：GetAssetTags() 非虚（:173）+ SetAssetTags 构造期锁（RF_NeedInitialization）+ CreateNewInstanceOfAbility check RF_ClassDefaultObject（:1199）+ FGameplayAbilitySpec(InAbility) 传实例在 InstancedPerActor 下必崩 → 行身份无法进 CDO。解法：Identity 片承载行 AssetTags（Row 删平铺字段）+ ActivateAbility 开头直写实例 AbilityTags（per-actor 独立无污染）+ 取消改 ApplyAbilityBlockAndCancelTags（引擎同款，替换裸 CancelAbilities）+ Statics 删 AddTag(AbilityTag)（DynamicSpecSourceTags=GE 源标签与身份正交）+ Gate 片数据源迁身份片；按 tag 检索对动态行失效记「不做」区；编译绿 d8f86b72（20:13）
- BlockAbilitiesWithTags 补齐（20:31，指纹 5f31d0b0）：Row 加行字段；Activate 施加 Block+Cancel 全量（:999 同款）；EndAbility 对称 UnBlock 回收（:888 同款，须配对非自动）
- BlockCancel 片化（20:37，指纹 af2335a7）：新建 FAssemblyFragment_BlockCancel 数据片（BlockTags+CancelTags），Row 删两平铺字段；DataDriven 加 GetBlockCancelConfig()，Activate/EndAbility 改读片；与 Identity 同构（数据在片、编排在 GA——USTRUCT 反射仅单继承，无法一片挂两流程点）







- include 教训：TInstancedStruct 头在 `StructUtils/InstancedStruct.h`（非 Templates/）；FOnGameplayAbilityEnded 需 `Abilities/GameplayAbilityTypes.h` 完整 include
