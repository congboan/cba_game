# 2026-08-13

## 治理层优化落地（governance-authoring 任务）

- build_editor.py 加指纹快路径：上次成功且指纹一致时跳过 UBT 返回 0，新增 --force 强制重建。已实测 skip 路径 exit 0。ADR 已追加。
- check_spec_required.py 修复 fail-open：state 缺失或损坏 → exit 2，与 harness fail-closed 一致。
- 新增伞形 spec specs/housekeeping.md（confirmed）：日常小修用，范围 Source/cba_game + Config + docs。
- 任务上下文已恢复 active_spec=settings-ui-framework。
- hook 载荷截断再实锤：2.3KB Edit 在 616 列截断；治理写入每段需小于 150 字符。
- root skill 认知区已同步：构建流程补快路径说明、截断阈值更新为 616 列实测。
- 文档漂移修复：README path_glob 速查表补 exempt_patterns。
- .workbuddy 消失事故根因确认：用户误删，非 junction（root skill 已更新）。
- 新观察：纯 ASCII Edit 稳过；含中文载荷截断风险高，每段 ≤150 列。
