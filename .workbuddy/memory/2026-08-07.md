# 2026-08-07 工作日志

## Spec 1 策略定稿（渲染与资产）
- 用户明确：渲染层全部 C++ 实现（USettingsScreenWidget/EntryWidget/ListView），Blueprintable 供生产蓝图子类继承做 UMG 绑定
- USettingCollection 用 UPrimaryDataAsset（AssetRegistry 可查）
- 爆炸半径收窄：只允许 Plugins/SettingsFramework/**，不动项目代码（Source/cba_game）
- spec 更新提交 d154167，已推送

## 重大变更：git 历史回退
- 用户终端执行 git reset --hard ae74ff3 + force push，撤销 0f12a35（C++ 框架全量）和渲染层未提交改动
- 现状：HEAD=d154167，插件仅骨架（uplugin+Build.cs+Module.cpp），VM 体系/Registry/CBASettingsLocal 均已移除
- harness_last_build 指纹已刷新（250 files, 8fe8fd25），编译通过

## 用户计划（新 session 交接）
- 用户将修改治理层：添加多 agent 编排（Planner/Coder/Reviewer/Explorer，源自 07-30 memory 历史规划但未落地）
- 新 session 目标：在治理层落地多 agent 机制后，再实现 SettingsFramework 插件功能
- 交接状态：spec 激活(confirmed)、工作区 clean、已推送

## 踩坑记录
- 引擎 UBA 编译故障（error 9001/9666）：环境性，重试即可恢复
- MVVM 关键结论：空 FFieldId 不注册（AddFieldValueChangedDelegate 内 IsValid 检查），必须用具体字段描述符
  （ThisClass::FFieldNotificationClassDescriptor::FieldName）；回调签名 TDelegate<void(UObject*, FFieldId)>
- UListViewBase::EntryWidgetClass 是 protected，C++ 设置需子类覆写 GetDefaultEntryClass()

## 下午：引擎解析重构 + 门禁修复 + git 治理（已提交 dfa8ad5 推送）
- 引擎死锁根因：门禁脚本模块级 import resolve_engine 崩溃 → exit 1 被误判命中 → 全量写入锁死。import 须放 try 内
- resolve_engine.py 重构（用户手写）：LauncherInstalled.dat + 注册表 Builds + 父目录搜索
- build_editor.py: -NoHotReload（解决 Live Coding 拒编）；find_engine allow_env=True
- 引擎选择：UE 5.8 Launcher 缺 Toolsets 插件 → 固定 D:/UnrealEngine(5.9)，Target.cs V8/Unreal5_9
- operation_normalizer.py: git add 归入 GIT_STAGING_SUBCOMMANDS（staging-only）→ git add -A 放行
- ⚠️ 事故：git add+commit 后 .workbuddy/ 目录在磁盘消失（root skill missing）→ git restore 恢复，无数据丢失
- 治理优化全量落地（4dafac4 + 0a00c5b）：gate 脚本规范、.workbuddy 保护、编辑载荷规避、git checkout 可解析、Live Coding 提示、scope_guard 崩溃vs命中区分（Traceback 检测）、恢复态 Glob
- 经验：WorkBuddy hook 对较大载荷持续截断（确定性 column ~250-1100），改 harness 关键文件用终端补丁脚本最稳（CRLF 注意）；scope_guard.py 工具侧 Edit 必被截断
- SettingsFramework 设计：Entry=单设置；Page 挂子项需 ChildEntries（未实现）；复杂逻辑需 ViewModelClass 扩展点（未实现）

## 治理层：新建 agent-parallel-work workflow skill
- 用户需求：治理层缺少多 agent 并行工作指导，AI 默认串行
- 创建 `.workbuddy/skills/agent-parallel-work/SKILL.md`（认知指导型，无 deny 约束）
- 内容：3 条硬规则、Agent 类型选型(Explore/general/Plan)、5 个并行场景模板、Team/独立模式选择、阶段适配(design/build/review/test)、反模式、执行规范
- 采用 governance-authoring 流程：临时切换 state → 分段写入(避 hook JSON 截断) → 恢复 state

## SettingsFramework 插件实现（specs/settings-framework.md）
- 完整实现 MVVM 设置系统插件（9 头文件 + 9 cpp 文件），编译通过（UBT Exit Code: 0）
- 文件清单：
  - 核心类型：SettingsFrameworkTypes.h（枚举/结构体）
  - 数据层：SettingEntry.h（UPrimaryDataAsset + 绑定路径）、SettingCollection.h
  - VM 体系：SettingViewModelBase.h/.cpp + Scalar/Bool/Enum/Action 子类 + SettingPageViewModel
  - 注册表：SettingRegistry.h/.cpp（LoadCollection/SaveChanges/Navigate/Search/RefreshTraits）
  - 渲染：SettingsScreenWidget(CommonActivatableWidget+UListView)、SettingsEntryWidget(IUserObjectListEntry)、SettingsListView(UListView)
- 踩坑：缺 PropertyPath 模块 → 改用手动反射(FindFProperty)；Initialize 与 UUserWidget::Initialize 冲突 → 重命名 SetupRegistry；Hook JSON 截断 → 全部分段写入
- .uplugin 补充插件依赖：ModelViewViewModel、CommonUI、CommonGame
