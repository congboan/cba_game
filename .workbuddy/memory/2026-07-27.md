# 2026-07-27

## hook protocol bug 诊断
WorkBuddy 5.3.5 HookExecutor 损坏含换行的 Edit/Write JSON payload，scope_guard.py 的 _HookProtocolError 已加 meta 参数，待手动应用 JSON decode handler 诊断捕获增强和 _hook_protocol_failure_report diagnostic 输出，待新建 probe_hook_stdin.py 探针。详细改动见对话。

## 三层架构约束落地
- 修复 `checks/check_gf_location.py`：从错误的全路径拦截改为内容分析（检测 `BuiltInInitialFeatureState` / `ExplicitlyLoaded: true`）
- 修复 `checks/check_gf_dependency.py`：修复 cwd 错误（原用 `os.getcwd()` 指向 skill 目录而非仓库根），改为 `../../..` 推导仓库根
- SKILL.md 新增 2 条约束：`project.arch.gf-location` + `project.arch.gf-dependency`
- SKILL.md 正文写入三层架构（Framework Plugins / Project Base / GameFeatures）决策树和硬/认知约束说明
- scope_guard --context 验证通过：12 条约束全部 effective，零诊断
- SKILL.md 新增 2 条资产保护约束：`project.asset.uasset-readonly` + `project.asset.umap-readonly`（`path_glob` deny `**/*.uasset` / `**/*.umap`），防止文件工具直接编辑二进制资产
- SKILL.md 正文新增资产操作认知约束：资产修改必须通过 MCP/UE Editor
- SKILL.md 正文新增「Experience Framework 工作流」节：核心数据资产、加载状态机、Pawn 初始化、GameFeatureAction 体系、关键类关系、依赖；新增「操作流程」子节：触发场景、创建/修改 Experience、新建 GameFeatureAction、验证方法