# 2026-09-01

## ue5 MCP provider tool_glob 覆盖缺口修复（治理变更）

- 现象：`mcp__ue5__list_toolsets` / `describe_toolset` 被 HARNESS_OPERATION_UNRESOLVED 拒绝；根因 = provider `tool_glob` 只精确匹配 `call_tool`，发现类工具无 provider 解释 → 语义事件全 unresolved → fail-closed 拒绝。
- 修复：root skill provider `tool_glob` 改为 `"mcp__ue5__*"`（命名空间通配，README 规范示例同型）；capability/effects/unresolved_events 不变；`pre_write` 证据依赖 `arguments.AssetPath`，发现类工具无此字段，无虚假写证据。
- 流程：`--preview-skills governance-authoring`（ready）→ state 切换选 governance-authoring（逐次 ask）→ 编辑 root skill（ask）→ `--explain` 验证 1/1 effective 零诊断 → 实测 `list_toolsets` 返回完整工具集 → state 恢复 aaf-named-slots → `--context` 零诊断。

## 环境事实（重要修正）

- Bash 裸 `python` 缺 PyYAML：`scope_guard --context` 报 12 条 frontmatter 诊断 + root_skill invalid + exit 2，形似治理源损坏，实为解释器缺依赖；治理源本身完好。
- `envs\default` venv 不存在；本会话 harness 脚本统一用 `D:\ProgramData\anaconda3\python.exe`（自带 yaml）绝对路径调用。
- WorkBuddy hook stdin 截断 bug 再现：约 1.5KB Write 载荷在 column 1561 截断 → `hook_payload.invalid_json` → 任务 abort；按 root skill 规范改分段小写入完成。
