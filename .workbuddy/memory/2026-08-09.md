# 2026-08-09 工作日志

## SettingsFramework：BindingPath 改造为 PropertyPathHelpers
- 单属性反射 -> FCachedPropertyPath + PropertyPathHelpers（Lyra 同款）
- Build.cs + PropertyPath 依赖；VM 基类换 FCachedPropertyPath
- Scalar/Bool/Enum 读值改统一字符串解析
- 新能力：函数链路径（GetLocalSettings.MasterVolume）、嵌套路径、失败静默
- 编译通过 280 files

## 未完成事项
- SettingsFramework 批次1-3 + PropertyPath + uproject 修复待提交
- .vsconfig 本地被删（非本次操作），待确认
- 批次4（项目侧具体类）已取消

## 编译死锁：UBA 缓存权限损坏 + NetCore 文件锁（5 次失败）
- 症状：build_editor.py 每次 ~25s 快速失败，Exit Code 6；Stop 门禁（project.build.stop）持续拦截一切写操作
- 日志铁证：`UnrealEditor-NetCore.dll/.pdb (Access is denied.)` → `LNK1201`；UBA 连 `C:\ProgramData\Epic\UnrealBuildAccelerator\.isNotAWS/.isNotGCP` 都写不了（ACL 损坏）；`Terminating git child process due to timeout`（引擎 git status 超时）
- 根因链：UBA 缓存目录权限损坏 → UBT 误判 NetCore 需重建 → dll/pdb 被进程锁（UnrealEditor/UBA 残留）→ 链接失败 → 指纹不刷新
- 处置：用户选择搁置。恢复方案（用户手动）：① 重启电脑（最可靠）② 管理员删 `C:\ProgramData\Epic\UnrealBuildAccelerator\` + NetCore dll/pdb + Intermediate
- 注意：build_editor.py UBT 参数硬编码，无 -NoUBA 透传入口

## 设计决策：BindingPath 属性选择器（方案 A 定稿，待实施）
- 需求：编辑器里 Entry 的 BindingPath 不能直接选属性，只能手写字符串
- 方案 A（已选）：`USettingCollection` 增加 `HostClass: TSubclassOf<UObject>`（最外层配一次），宿主类约定静态 `Get()`（Lyra 式，如 `UCbaSettingsLocal::Get()`），Registry 反射调用拿实例；BindingPath 相对宿主类（如 "MasterVolume"）
- 属性选择器：给 BindingPath 定制编辑器（IPropertyTypeCustomization），基于 `Collection->HostClass` 枚举属性，选中回填字符串，运行时零改动
- 改动范围：SettingCollection.h（+HostClass）、SettingRegistry（宿主解析 InHost → HostClass::Get()）、新增编辑器 Customization；Entry 无需改动
- 待编译恢复后实施（当前 Stop 门禁拦写操作）

## 设计决策：Page/Group 语义对齐 Lyra（已确认，待实施）
- Lyra：普通分组 = UGameSettingCollection（IsSelectable=false）；可点选子页 = UGameSettingCollectionPage（IsSelectable=true）；叶子 = Value 类
- 当前 SettingsFramework 问题：二级分类全用 Page 类型 Entry 表达，Page 语义过载
- 改动 1：`ESettingValueType` 新增 `Group`（对应 Lyra Collection，不可点选分组标题）
- 改动 2：VM 增加 `bSelectable` 标记（Group=false / Page=true），ListView 点击行为按此区分
- 改动 3（**确认做**）：Collection 资产本身生成分组节点——`LoadCollectionInto` 为每个 USettingCollection 生成 Group VM（用 Collection 的 DisplayName），再挂子项，顶级分类也是节点，与 Lyra 完全一致
- 影响：SettingsFrameworkTypes.h、SettingViewModelBase.h、SettingRegistry.cpp、SettingsScreenWidget/ListView

## 已完成：改动 1-5 全部落地（编译通过 285 files）
- **改动1**：`ESettingValueType` + `Group` 枚举（SettingsFrameworkTypes.h）
- **改动2**：VM 基类 + `bSelectable`（FieldNotify，SetSelectable/IsSelectable），Page=true / Group=false
- **改动3**：`LoadCollectionInto` 为每个 Collection 生成 Group VM 节点（CollectionName 作标题），Entry 挂到 CollectionVM 下；Entry->ChildPages 递归挂 PageVM；Collection->ChildPages 递归挂 CollectionVM
- **改动4**：`SettingsListView::OnItemClickedInternal` 覆写——仅 IsSelectable 广播 OnEntryActivated；Screen SetupRegistry 时绑定导航。注意：UListView 没有 HandleListEntryWidgetClicked，正确虚函数是 OnItemClickedInternal(UObject*)
- **改动5**：Collection + `HostClass: TSubclassOf<UObject>`；Registry `ResolveHost`（反射静态 Get()）；`FSettingBindingPath` 结构体替代裸 FString（隐式转 const FString&）；新增 SettingsFrameworkEditor 模块（uplugin Type=Editor）+ `FSettingBindingPathCustomization`（SComboButton + FMenuBuilder 枚举 HostClass 属性回填）
- **编译经验**：FMenuBuilder::BeginSection 首参是 FName 非 FText；AddMenuEntry 用 (FText,FText,FSlateIcon,FUIAction) 4 参形式；CreateRaw 回调参数按值不按 const&
