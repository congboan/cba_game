# 2026-08-28 工作日志

## AAF tag 矩阵 7 字段全量归位（Gate 7-tag + OwnedTags 片）
- 实证修正：Source/Target Tags 非遗留，引擎 :411-426 真实消费
- SourceTags 数据流 = TriggerEventData.InstigatorTags（ASC :1813-1814）
- Gate 片扩展四门：SourceBlocked/SourceRequired/TargetBlocked/TargetRequired
- 判定序列对齐引擎：全 blocked → 全 required → AssetBlock（:432）
- 失败写 ActivateFailTags 原因 tag（:361-401 同款 lambda）
- OnCanActivateBase 签名首次演进：+ SourceTags/TargetTags 指针
- 新建 OwnedTags 片（ActivationOwnedTags 语义，纯数据 + GA 编排）
- Activate 施加 AddLooseGameplayTags（:990）/ End 对称移除（:870），复制走 ShouldReplicateActivationOwnedTags
- 编译绿：指纹 0d1a1592（318 文件），--force 实证（自动编译已覆盖，UBT Target is up to date）
- spec 勾稽更新（含 BlockCancel 片化补回填）；selector 恢复确认

## 数据片访问模式统一（用户纠错，13:27）
- 旧写法三错：Indices[0] 只取首个、static_cast 挖字段、数据片硬套查表语义
- 正解：数据片自带聚合入口（CollectTags 等），GA 编排 ForEachHook 捕获外部容器逐片聚合
- GetIdentityTags/GetActivationOwnedTags value-return aggregate; GetBlockCancelConfig removed
- HookIndices key = dispatch type; fingerprint 96ab585d (--force verified)

## ForEachHook static_cast 移除（用户纠错，13:31）
- GetPtr<HookT>() 本身类型安全（内部 IsChildOf 检查），static_cast 多余且有 UB 隐患
- const 语义确认：片运行时只读，运行时分派路径全 const（8 钩子 + Collect* + Check/Apply）；
  唯一非 const = Precache/ContributeToSpec（授予期工具，语义正确）
- Fn 收 const HookT&；指纹 a8ef808e（--force 实证）

## Tags 片整合（用户定案，13:52）
- 引擎 9 tag 字段合一 FAssemblyFragment_Tags（Cancel/BlockAbilitiesWithTag、ActivationOwnedTags、
  ActivationReq/Blocked、SourceReq/Blocked、TargetReq/Blocked + bCheckAssetBlock）
- 每字段一个 Collect 函数（AppendTags 风格）+ CollectTags 全集聚合
- Gate/BlockCancel/OwnedTags 三片删除（字段迁入）；判定统一 GA 内部（CanActivateAbility 内联
  引擎 7 项序列 :349-443 同款）；OnCanActivateBase 钩子保留框架无消费者
- 指纹 87b7746e（315 文件 = 318-3，--force 实证）

## AssetTags 并入 Tags 片（13:56）
- Identity 片删除，AssetTags 并入 FAssemblyFragment_Tags（+ CollectAssetTags + CollectTags 全集含入）
- GetIdentityTags 改读 Tags 片 CollectAssetTags；实例直写/AssetBlock 判定路径不变
- 引擎同组依据：AssetTags 与 9 字段同在 GameplayAbility.h Tags Category（:475 vs :738-771）
- 指纹 7faaea49（314 文件 = 315-1，--force 实证）

## 插件目录整理（15:20，用户定案①②③）
- 去掉 Experimental/ 前缀（include 全量更新，grep 残留清零）
- Fragment/Execution 拆子目录；UAssemblyAbilitySource/AssemblyAbilityStatics 独立 Assembly/ 目录
- 40 文件全量归位；新旧无并存（Write 新路径 + rm 旧路径；mv/git mv/cp/python rename 均被 harness 拦，
  文件移动只能走结构化工具链 Read+Write+rm）
- 大文件 Write 触发 stdin 截断（AssemblyAbilityStatics.cpp/DataDriven.cpp）→ ASCII 化注释分刀 Write+Edit 追加
- 编译：首轮 UHT generated 文件锁失败（Failed to rename exported file）→ 并行 UBT/editor 释放后重试
  成功，指纹 5cfef8a7（314 文件，15 编译单元）

## K2_SendChainEvent 删除（15:23，用户定案）
- 蓝图发链事件不需要 K2 包装：引擎 SendGameplayEvent 节点直接填装 FGameplayEventData
  （InstancedEventData = FAssemblyChainContext payload），数据都在 EventData 中
- 保留 C++ SendChainEvent（深度推进/根 ID/安全检查 + HandleGameplayEvent）；GetChainContext 保留
- 指纹 1083a55f（--force 实证）

## 链上下文载体版本适配（15:36，用户定案）
- FGameplayEventData::InstancedEventData 为 5.9 新增字段；插件要兼容 <5.9 → 内部适配层
- 版本宏 AAF_HAS_INSTANCED_EVENT_DATA（FAssemblyChainTargetData.h）；API 归 UAssemblyAbilityStatics 静态：
  SetChainContext / GetChainContextFrom / GetTargetActors（5.9+ Instanced / <5.9 TargetData）
- 新 FAssemblyChainTargetData（TargetData 官方即多态扩展容器；NetSerialize 四字段；基类 NetSerialize
  非虚不加 override；SerializeObject 需 UObject*& 4 参；UHT 禁止 USTRUCT 在预处理器块内 → 类始终编译）
- SendChainEvent/GetChainContext/执行策略（InstantGE/AOE 取目标）内部改用适配 API，使用者零感知
- 指纹 3f78ae24（315 文件，--force 实证）

## SendChainEvent/GetChainContext 删除（16:04，用户定案）
- UGA_AssemblyBase::SendChainEvent + GetChainContext + MaxChainDepth 全删
- 链深度推进/根 ID/安全检查机制整体退役（过度设计）
- 执行策略发链事件改为内联：拷贝父 EventData（继承链上下文载体，5.9 值拷贝 / <5.9 TSharedPtr 共享）
  → 改 EventTag/Instigator/Target → HandleGameplayEvent
- 载体适配 API（SetChainContext/GetChainContextFrom/GetTargetActors）保留
- UGA_AssemblyBase.cpp 重写（ASCII 化注释规避中文 TEXT 行截断）
- 指纹 2230eb07（--force 实证）

## 提交推送（16:12）
- commit a33c19f（47 文件 +2442/-930）：refactor(AAF) 引擎 tag 矩阵片化整合 + 目录整理 + 链上下文版本适配
- git 自动识别 Experimental 去前缀移动为 rename（利于追溯）
- push origin main：fdb9b7d..a33c19f


















