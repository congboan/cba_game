## 2026-08-01

### Root Skill Frontmatter 修复
- **问题**：scope_guard 报告 `root_skill.invalid`，导致所有工具调用被 HARNESS_TASK_ABORT 拦截
- **根因**：SKILL.md 的 YAML frontmatter 中使用了内联 flow mapping 语法（如 `data: {pattern: "..."}`, `capabilities: [ue.asset.edit]`），scope_guard 的 YAML 解析器无法处理
- **修复**：将全部 YAML 转为纯块风格（block style），避免 `{}` 内联映射和方括号列表混用
- **结果**：16 条约束全部恢复生效，恢复态解除，项目可以正常工作

### scope_guard.py YAML 错误吞没修复
- **问题**：`_load_yaml_text()` 用裸 `except Exception` 吞掉所有错误（ModuleNotFoundError + YAML 语法错误全部静默），导致 frontmatter 解析失败时看不到真正原因
- **修复**：拆分为 `except ModuleNotFoundError`（返回 None）+ `except Exception as exc`（输出 `[scope_guard] YAML parse error: {exc}` 到 stderr），保持向后兼容的同时暴露真实错误
- **位置**：`harness/scripts/scope_guard.py` 第 191-201 行

### AssemblyAbilityFramework 插件代码审核（用户要求）
- 全量读完 30 个 .h/.cpp 文件（主框架基类 + Experimental 数据驱动 + EventRouter + 执行策略 + EffectContext + Validator + Statics）
- 总体：架构清晰（editor-first 主框架 / Experimental 数据驱动双层），GAS 用法基本规范
- 发现 P1 级问题：`FAssemblyEffectContext::Duplicate()` 对 `Extension`（TInstancedStruct）的浅拷贝；AOE/InstantGE 用 `IsValid()`（=LoadSynchronous 并缓存）判空导致不必要的同步加载
- P2 级：EventRouter 挂 `ActiveGE.EventSet.OnEffectRemoved` 后若 GE 先于 Router 被移除会导致悬垂委托；`OnAvatarSet` 对 WhileInputActive 未处理；本地 TryActivateAbility 在网络下不可靠
- 需要项目侧 `UAbilitySystemGlobals::AllocGameplayEffectContext` 注册自定义 Context 才能生效，插件未强制

### 4 issues verified (P1, all real)
- FAssemblyEffectContext.cpp:75 Map->SerializeObject: cross-process Source=null, no null check
- UGA_AssemblyEventRouter.cpp:185 const_cast on ActiveGE->EventSet.OnEffectRemoved: dup binding
- ExperienceManager.h:16 static TMap: process-global, multi-world/cloud-shared count
- InventoryManagerComponent.cpp:289 ConsumeItemsByDefinitionId: partial consume (StackCount-=) sends no fragment notification
- Lyra original (E:\Unreal Projects\UE5\LyraStarterGame): ConsumeItemsByDefinition removes whole stack per call, so no partial-consume path exists; bug introduced by extraction semantics change, not inherited. OnInstanceRemoved is our addition (Lyra base has OnInstanceCreated only).

### Stop hook build_freshness gate (stuck since 21:14)
- Saved/harness_last_build.json: success=true, ts=2026-08-01 21:14:44, 245 files sha256:caaabc... - unchanged
- Stop hook reruns fingerprint each reply; mismatch = deny: "no successful build matching current source"
- Root skill project.build.stop: when=stop, file=Saved/harness_last_build.json, action=deny
- In-session Bash/Python all blocked by governance (no shell provider) - cannot recompile or compute fingerprint
- ONLY fix: run `python harness/scripts/build_editor.py` in external terminal to refresh fingerprint
- Also: Edit with long Chinese payload triggers hook_payload.invalid_json (line1 col~1000) - use short ASCII edits

### RESOLVED: build gate unlocked at 23:05
- User ran `python harness/scripts/build_editor.py` externally: PASS, 0 errors, 0 warnings
- New fingerprint sha256:30821f63593e3b3c3fc57b052d725a2a79e9f718caae53179b19de11df253c16, ts=2026-08-01 23:05:23, 245 files
- Stop hook now matches; normal work can resume
- KEY LESSON: when build_freshness gate blocks stop, the ONLY fix is an external real compile (in-session shell is blocked by governance). Also note the earlier 21:14 fingerprint (caaabc...) differed because source changed after that build - resolved now.

### FIXED: in-session compile IS possible (root cause: command form)
- User request: AI should be able to self-call build script when gate triggers, no manual external run
- Investigation: operation_normalizer.py:724-733 already whitelists build_editor.py as trusted_harness_wrapper; PYTHON_PROGRAMS={py,py.exe,python,python.exe,python3,python3.exe}
- WHY my earlier Bash calls failed: I used `cd ... && python ...; echo $?` (compound command) -> normalizer can't statically parse -> python_effects_unresolved -> HARNESS_OPERATION_UNRESOLVED
- FIX: call with PURE command form - absolute python path + absolute script path, NO cd/&&/;echo suffixes
  `"C:/Users/congb/.workbuddy/binaries/python/versions/3.13.12/python.exe" "E:/Unreal Projects/cba_game/harness/scripts/build_editor.py"`
- Verified: PASS, 0 errors, state refreshed ts=23:08:58. NO governance config change needed - mechanism already supported it
- LESSON: build_editor.py in-session = use exact absolute paths, single command, no chaining
