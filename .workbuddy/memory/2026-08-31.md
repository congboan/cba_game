# 2026-08-31 work log

## SharedRoot -> UpdateOverlaps full-child recursion (Insights profiling Q&A)
- User profiled a merged-BP actor: SharedRoot (Component) -> Set (2) -> UpdateOverlaps -> 16+ child components each doing overlap work
- Chain: Set* on collision-enabled primitive -> MoveComponentImpl -> bMoved -> UpdateOverlaps (PrimitiveComponent.cpp:3507-3545)
- UpdateOverlapsImpl (PrimitiveComponent.cpp:4474-4675):
  - per-component query gated on GetGenerateOverlapEvents() && IsQueryCollisionEnabled() (:4490)
  - root component uses bIgnoreChildren=true in its own query (:4498, comment :4496-4497: children update when we descend)
  - actual scene query per component: ComponentOverlapMulti (:4534-4543, STAT_PerformOverlapQuery)
  - child recursion (:4652-4665) is UNCONDITIONAL - descends AttachChildren regardless of flags
- Design: parent move is the single dispatch point refreshing whole subtree overlap state (children world transforms changed via attachment)
- Fix levers: SetGenerateOverlapEvents(false) on children that don't need it (early-out at :4490); narrow collision channels; fewer attached collision components
- Note: in 5.9 OnUpdateTransform no longer calls UpdateOverlaps (only SendPhysicsTransform, :1062-1084); the MoveComponent path is the overlap dispatch point

## Follow-up: user patched out root-move child descent, asks if semantics break
- User skipped child descent on root move; cost gone. Question: don't children still overlap?
- Answer: overlap discovery is "moving side discovers, BOTH sides get notified" - BeginComponentOverlap does reflexive bookkeeping on both lists (PrimitiveComponent.cpp:4137-4138) and dual-broadcasts OnComponentBeginOverlap (:4145 self + :4150 other, "we are the one that moved"), dual actor notify (:4154-4165)
- Safe iff every overlap pair has at least one self-refreshing side (moving character discovers your static child). Missed case: your root carries child INTO/OUT OF a never-refreshing object (static trigger) - Begin/End silently lost
- Engine's legit lazy version: bSkipUpdateOverlaps flag (SceneComponent.h:165-167, wrapper SceneComponent.cpp:1087-1100 caches verdict; aggregated from children via &= at PrimitiveComponent.cpp:4663; CVar p.SkipUpdateOverlapsOptimEnabled). Never engages for trees whose children have GenerateOverlapEvents=true - user's patch is a stronger gameplay assumption than the engine's

## Clarified setup: actor with many ChildActorComponents, root ignores them -> cost gone
- Real setup: one actor, many ChildActorComponents, each child actor has many StaticMeshComponents; moving root = the Insights cost; root->IgnoreActorWhenMoving(all child actors) fixes it
- Key mechanism: MoveIgnoreActors is a QUERY filter injected by InitSweepCollisionParams (PrimitiveComponent.cpp:3214) into BOTH the movement sweep (ComponentSweepMulti :3364, params :3358-3360) AND overlap refresh query (:4542-4543); hits die in broadphase, never become results
- Sweep collects touch hits into PendingOverlaps (:3409-3430) feeding UpdateOverlaps (:3538); root shape covers whole cluster -> N x M hits; diff loop O(Old x New) linear scans (:4584-4596)
- ShouldIgnoreOverlapResult (:3180-3208) has NO same-actor / attachment filter - only self, overlap flags, WorldSettings, uninitialized
- Children's own queries cheap: small shapes, bounded results, parent-move teleports children (no sweep, :3502 comment), most static meshes lack GenerateOverlapEvents (early-out :4490); dual bookkeeping churn (BeginComponentOverlap :4137-4138) disappears with root-side ignore

## NetworkPredictionProxy: InitForNetworkRole vs Init<ModelDef>
- InitForNetworkRole (NetworkPredictionProxy.h:60-68): caches CachedNetRole + bCachedHasNetConnection; if ConfigFunc installed (post-Init) triggers EConfigAction::UpdateConfigWithDefault. Role -> local behavior via FNetworkPredictionDriver::GetConfig (NetworkPredictionDriver.h:112-143): Authority = Passive w/ net connection (input arrives via replication) else PollPerSimFrame; AutonomousProxy = PollPerSimFrame + AP LOD; SimulatedProxy = Passive + SP LOD
- Init<ModelDef> (NetworkPredictionProxyInit.h:13-90): gets UNetworkPredictionWorldManager, builds default Archetype + SimulationID (client temp ID remapped later via ConfigFunc), WorldManager->RegisterInstance(Simulation, Driver, &View), installs ConfigFunc lambda = type-erased bridge from untemplated proxy into templated ModelDef world (EndPlay/UpdateConfigWithDefault/trace/ID remap); captures ReplicationProxySet (Input/Sync/Aux); config lands only when "role cached && ID>0" (:78-81)
- Caller of InitForNetworkRole in 5.9: UNetworkPredictionComponent::CheckOwnerRoleChange (NetworkPredictionComponent.cpp:77-94) on server PreReplication / client PreNetReceive when owner role/connection changed; NetworkPredictionProxy itself is replicated (:66)
- 5.9 Mover InitializeNetworkPredictionProxy only calls Init (MoverNetworkPredictionLiaison.cpp:360-369); user quoted 5.7-era two-line version - semantics unchanged
- 5.9 ordering: UNetworkPredictionComponent::InitializeComponent (NetworkPredictionComponent.cpp:16-34) = RepProxies init -> InitializeNetworkPredictionProxy() [Init] -> CheckOwnerRoleChange() [first InitForNetworkRole]. So 5.9 = Init FIRST, role SECOND; config lands immediately on server (real ID), deferred on client until replicated ID via ConfigureAfterLoading (temp ID fails the ID>0 gate at NetworkPredictionProxyInit.h:78). 5.7-era quoted order was role first, Init second - landing deferred to later triggers; no hard dependency between the two calls either way
- this, this args = ModelDef::Simulation* and ModelDef::Driver* (liaison plays both in Mover)
