# 2026-09-07

## CommonUser 可删性分析（两轮调研，结论：保留并切 OSSv2）
- 依赖链：cba_game.Build.cs:11 → CommonGame（Build.cs:23 PublicDependency + uplugin 声明）→ CommonUser。删 CommonUser 连锁编译失败。
- 引擎 d:\unrealengine 为裁剪版：Engine/Plugins/Runtime 下无任何 Common* 插件（CommonUI/CommonUser 均无），本地 Plugins/CommonUser 是唯一来源且为汉化定制版。
- CommonGame 唯一 CommonUser 耦合点：CommonGameInstance.cpp（SetTraitTags L89-95、ResetUserState L108-111、FCommonUserTags L27/L137、include L8）。
- 跨平台 trait 真正源头在 CommonUI：CommonGameInstance.cpp:87 `ICommonUIModule::GetSettings().GetPlatformTraits()`，CommonGame 只搬运，CommonUser 只存储（3 处 Platform_Trait_SingleOnlineUser 判定：cpp:2498/2516/2556，本地多用户逻辑，与 EOS 无关）。
- OSS 双轨：COMMONUSER_OSSV1 宏分布 Subsystem 25 处 / SessionSubsystem 44 处；总量 ~5700 行。**当前构建走 OSSv1**（CommonUser.Build.cs:11 bUseOnlineSubsystemV1=true，COMMONUSER_OSSV1=1），OSSv2 分支完整但未编译（UE::Online::IAuth/IConnectivity/ISessions/ILobbies 全套）。
- 决策：用户要做 EOS 联机 → 保留 CommonUser，路径 = Build.cs:11 翻 false 切 UE::Online + OnlineServicesInterface 依赖 + EOS 插件配置；删除则需自重写 1769 行会话封装。纯单机才值得剥离（需新 spec，动 CommonGame 公共 API）。

## CachedTraitTags 考古
- 写入：cpp:2550（SetTraitTags 纯赋值无广播）；唯一喂入口 CommonGameInstance.cpp:92。
- 读取全在 CommonUserSubsystem 内部，只消费 Platform_Trait_SingleOnlineUser；对外 GetTraitTags/HasTraitTag（h:358/362）项目内零调用。

## 相机系统选型登记（治理写入 root skill）
- 现状核查：全仓零相机系统；用户选型 GameplayCameras 为唯一相机系统。
- 已写入 root skill 相机系统选型章节，HeroComponent 暂不实现。
- 流程：预检→state 任务切换→分段 Edit→验证→恢复，全程无诊断。
- 新发现：hook 截断并非纯偶发——载荷含中文引号时稳定在固定列数截断（399），去掉引号后同长度载荷立即成功。

## 输入 GA 化讨论（SendGameplayEvent 通道，5.9 源码核实）
- 值通道成立：FGameplayEventData 有 EventMagnitude(float)/TargetData/InstancedEventData(FInstancedStruct, 5.9 新增, GameplayAbilityTypes.h:264-272)。
- 两条消费模式必须分清：AbilityTriggers(GameplayEvent) = GiveAbility 时注册索引(AbilitySystemComponent_Abilities.cpp:577,592)，每事件走 InternalTryActivateAbility 完整激活管线(:2527)，已激活再触发被丢弃——不能用于每帧输入；WaitGameplayEvent task = GenericGameplayEventCallbacks.FindOrAdd 精确订阅(AbilityTask_WaitGameplayEvent.cpp:35)，不走激活管线——每帧输入唯一正确形态。
- 5.9 HandleGameplayEvent(:2564) 用 TMap 索引+tag 父链冒泡(:2571,:2584)，非全量遍历；反而 Lyra AbilityInputTagPressed(:186) 才是全量扫 ActivatableAbilities（有 tick 缓冲批处理）。
- Mover 场景网络恰好成立：服务端只吃 input cmd replay，不需要原始输入值，payload 不复制无碍。
- 结论倾向：move/look 留 native（Mover input producer 薄胶水）；若要输入全数据驱动，走常驻 GA + WaitGameplayEvent + InstancedEventData 传值，代价 = GA 激活语义全旁路 + 事件通道每帧噪音，需立法禁止给 InputTag.* 配 AbilityTriggers。
