# 2026-08-11

## hook 截断根因确认（决定性实验）

- 实验：Write 410 字符载荷 → `hook_payload.invalid_json` **column 410** = 载荷长度处截断。
- 结论：**WorkBuddy 客户端 hook 传输层偶发截断**（截断点随载荷长度漂移，非固定阈值），载荷越大/中文越多越易触发，连续调用与 PostToolUse 编译并发时加剧。harness 侧无缺陷。
- 对策：小步写入（<200 字符）、避免长中文、再遇重启客户端。

## 治理修复收尾

- selector 恢复 GFTests：`{"stage":"build","active_spec":"specs/gf-tests.md","active_skills":[]}`，--context 验证通过（无诊断）。
- spec.gftests.allowed-path 豁免生效：任务切换/治理编写/memory 不再被半径锁死，治理死锁根治。
- 待办：编译门禁未恢复（指纹 failed + Saved 残留 3 个 .tmp + WinError 5 写指纹失败）——需重启电脑后编译刷新。

